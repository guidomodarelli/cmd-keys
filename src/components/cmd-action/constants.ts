export const Attributes = {
  ACTION: "action",
  SELECTED: "selected",
  HOT_KEYS_JOINED_VIEW: "hot-keys-joined-view",
} satisfies Record<string, Lowercase<string>>;

export const CmdActionEventType = {
  ACTIONS_SELECTED: "actions-selected",
} satisfies Record<string, Lowercase<string>>;
